// Top-level build file for the Skin Lesion Screener Android app.
// Versions are pinned for reproducibility; bump in Android Studio when needed.

plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
