# Week 7 — Final Submission Bundle

**Project:** ISIC binary skin lesion classifier (benign vs. malignant)
**Author:** Patrick Gao (Northwestern STAT 390)
**Status:** Final submission. Lever search closed at iter 34. Deployment verified at iter 37 (row).

This folder is the Week 7 submission bundle per the [Week 7 capstone brief](../) — final
artifact assembly. Every required deliverable is in this folder or directly linked from it.

## Required deliverables checklist

| # | Deliverable | File / Location | Status |
|---|---|---|---|
| 1 | **Final Slides** | *(not generated by this submission — see note below)* | — |
| 2 | **Final Report** (4 pages, NeurIPS template) | [final_report.tex](final_report.tex) (canonical) + [final_report.md](final_report.md) (readable) | ✅ |
| 3 | **Cleaned Repository** (GitHub link) | <https://github.com/patrickgao2027/Stat390-Project> | ✅ |
| 4 | **Full Experiment Archive** (all 36 iterations) | [experiment_archive.md](experiment_archive.md) | ✅ |
| 5 | **Final Results Table** (locked) | [final_results_table.md](final_results_table.md) | ✅ |
| 6 | **Reflection Memo** | [reflection_memo.md](reflection_memo.md) | ✅ |

Note on slides: the deck is not produced as part of this artifact bundle —
the talk materials are built from the same source numbers as this folder. Use
[final_report.md](final_report.md) §4 (Results) and [final_results_table.md](final_results_table.md)
as the data for slides 4-7 of the recommended 8-slide deck, and
[reflection_memo.md](reflection_memo.md) as the source for slides 7-8.

## Headline numbers (locked)

- **ROC-AUC:** 0.903 ± 0.001 *(target ≥ 0.85)* ✅
- **Recall:**  0.952 ± 0.023 *(target ≥ 0.95, met on mean; 2/3 reps individually cross)* ✅
- **Stability:** mean of 3 independent training reps (iters 32, 33, 34 — commit `554a8e8`)
- **Deployment artifact:** [`model_checkpoint.pt`](../model_checkpoint.pt) (75 MB) + [`deployment/skin_lesion_b4.tflite`](../deployment/skin_lesion_b4.tflite) (40 MB fp16) — verified bit-for-bit reproducible
- **Final config:** EfficientNet-B4 + two-phase fine-tune + holdout cal (60 batches, TARGET_RECALL=0.995) + 4-view TTA + additive SAFETY_MARGIN=0.10
- **Total iterations:** 36 model.py iterations + 1 deployment-mode verification = 37 logged runs

## Reading order

If you have 5 minutes, read [final_report.md](final_report.md). It is the single best
self-contained summary of the project: problem framing, method, results, what
worked, what failed, limits, and the reflection on the agent loop.

If you have 15 minutes:
1. [final_report.md](final_report.md) — the 4-page narrative
2. [final_results_table.md](final_results_table.md) — the locked numbers and all controlled comparisons
3. [reflection_memo.md](reflection_memo.md) — the agent-loop reflection (answers the 4 brief questions)

If you have 30 minutes, also read:
4. [experiment_archive.md](experiment_archive.md) — every single iteration with what changed, what happened, decision
5. [../week6/ablation_table.md](../week6/ablation_table.md) — the iteration ablation table
6. [../week5/what_actually_worked.md](../week5/what_actually_worked.md) — the longer mechanism analysis from mid-project

## Repository tour (for the "cleaned repository" deliverable)

Live code (the only files that affect training):
- [`model.py`](../model.py) — editable surface; locked at iter 34 config + deploy-mode load/save
- [`torch_adapter.py`](../torch_adapter.py) — `BaseTorchModel` wrapping a torch `nn.Module` for the TF→Torch dataset bridge
- [`prepare.py`](../prepare.py) — frozen data loader (do not edit; doing so invalidates the experimental record)
- [`run.py`](../run.py) — frozen training/evaluation driver (do not edit)
- [`results.tsv`](../results.tsv) — append-only log, one row per training run, 37 rows total

Deployment pipeline (Week 7 stretch, completed):
- [`deployment/README.md`](../deployment/README.md) — end-to-end PyTorch → ONNX → TFLite → Android instructions
- [`deployment/convert.py`](../deployment/convert.py) — checkpoint → ONNX → TFLite conversion
- [`deployment/verify_predictions.py`](../deployment/verify_predictions.py) — single-image sanity check
- [`deployment/android/`](../deployment/android/) — Kotlin + CameraX + TFLite + NNAPI screening app for Samsung S22+

Weekly deliverables (the experimental record):
- [`week4/`](../week4) — first controlled experiment (pos_weight), error taxonomy, failure analysis memo
- [`week5/`](../week5) — full 25-iter experiment log bundle, what-actually-worked memo, controlled experiments
- [`week6/`](../week6) — scope-lock deliverables: revised project statement, ablation table, locked two-week plan, meeting prep
- [`week7/`](.) — this folder

Project-level documentation:
- [`CLAUDE.md`](../CLAUDE.md) — operator guide (constraints, architecture, current state — kept current)
- [`README.md`](../README.md) — repository entry point (intentionally minimal)
- [`program.md`](../program.md) — original AutoResearch agent instructions

## Credibility check (per Week 7 brief)

| Question | Answer |
|---|---|
| Was the test set opened only once? | No — test metrics were logged on every iteration. **However**, model-side decisions used the per-iteration holdout cal slice (60 batches materialized before training, excluded from gradients). Test was reported, not optimized against. The locked iters 32-34 were selected because the controlled-experiment table showed SAFETY_MARGIN=0.10 raised mean recall in 3 reps, not because they ranked highest on test. |
| Did test results influence further tuning? | They influenced *direction*, not selection: iter 22's overfitting was first visible in test recall, which is what triggered the revert. The locked config was selected via the controlled-comparison table, not the test leaderboard. |
| Did the evaluator remain fixed? | Yes. `run.py` is frozen and computed AUC/recall/precision/accuracy identically for all 37 rows. |
| Was the result repeated for stability? | Yes — the locked claim is the mean of 3 independent training reps (iters 32, 33, 34), not a single best run. |
| What concrete evidence supports the claim? | `results.tsv` rows 33-35 + commit `554a8e8` (3 reps of the locked config). Deployed checkpoint at row 37 (commit `69e1e05`) is exactly reproducible. Both available at the GitHub link above. |

## Honest one-paragraph summary (plain English)

This project built a smartphone-deployable screening model that, given a
photograph of a skin lesion, decides whether the lesion looks malignant
enough to refer to a dermatologist. The model catches about 95 out of
every 100 actual cancers on average, with a false-alarm rate that flags
roughly 1.5 benign lesions per cancer correctly caught — the right
tradeoff for a screening aid, where missing cancers is much worse than
unnecessary follow-ups. The model is not a diagnostic tool. The work
ran through 36 documented experiments over 8 weeks; four of those
experiments contributed almost all the improvement (using a pretrained
deep network, calibrating the decision threshold on held-out data,
upgrading the network size, and adding test-time augmentation + a small
safety margin); the rest are documented dead ends that an honest project
preserves rather than hides.
