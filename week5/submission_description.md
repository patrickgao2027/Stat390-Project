# Week 5 Submission — Index

**Author:** Patrick Gao | **Project:** ISIC binary skin lesion classifier (benign vs. malignant) | **Block:** iters 1–21

This is the autonomous-block trace for Week 5. The block is **21 completed iterations + 1 crash recovered = 22 attempts**, spanning ~16 hours of training, three replicated single-variable controlled experiments, and a determinism test. The trace meets all four Week-5 quality standards (logs complete, changes interpretable, rollback logic clear, experiments comparable) — see the deliverables below.

## The 5 required deliverables

| # | Deliverable | File |
|---|---|---|
| 1 | **Complete Experiment Log Bundle** | [`experiment_log_bundle.md`](experiment_log_bundle.md) — every run with commit, status, change description, metrics, training time, and the operational events not visible from `results.tsv` alone (worktree crash, stdout-buffering false alarm, `keep`/`discard` mismatch on iters 8/9). |
| 2 | **Metric Trajectory Plot** | [`metric_trajectory.png`](metric_trajectory.png) — ROC-AUC and recall across iters 1–19, color-coded by run status, with the three controlled-experiment sub-blocks shaded. Companion: [`controlled_experiments.png`](controlled_experiments.png) — per-condition mean ± std bars for the three controlled sets. Generated by [`make_plot.py`](make_plot.py). |
| 3 | **Keep / Discard / Crash Summary** | [`keep_discard_crash_summary.md`](keep_discard_crash_summary.md) — 17 keep / 2 discard / 1 crash. Companion bar chart: [`keep_discard_crash.png`](keep_discard_crash.png). |
| 4 | **Best Result vs. Baseline** | [`best_vs_baseline.md`](best_vs_baseline.md) — baseline AUC 0.793 → block AUC 0.898 ± 0.003 (real, reproduced). Recall target ≥ 0.95 *crossed twice as singletons* (iter 13, iter 15) but **not** reproducibly. |
| 5 | **"What Actually Worked" Memo** | [`what_actually_worked.md`](what_actually_worked.md) — 3 modifications worked, 4 didn't, with measured effect sizes and replication evidence for each. The headline finding: the threshold-calibration `min(positive_prob)` estimator was the dominant noise source; replacing it with a percentile cut recall std 2.8×. |

## Lightning-round answers (≤ 90 seconds)

| Question | Answer |
|---|---|
| **Block length** | 22 attempts, ~16 hours of training |
| **Best result vs baseline** | AUC 0.793 → 0.898 ± 0.003 (real, n≈20 reproductions). Recall 0.898 → ~0.83–0.92 mean depending on estimator — close to the 0.95 target but not reproducibly past it. |
| **Keep / Discard / Crash rates** | 19 / 2 / 1 (out of 21 completed) = 90 % / 10 % / 5 % |
| **Most helpful modification type** | (a) Architecture upgrade (LR → pretrained CNN); (b) introducing a threshold-calibration mechanism with default hyperparameters; (c) replacing the noisy `min` threshold estimator with a percentile (variance-tightening) |
| **Biggest current uncertainty** | The residual recall variance is structural — `prepare.py`'s frozen data pipeline cannot be seeded from model.py (confirmed by iters 20–21: identical code, different results). The two remaining levers are percentile tuning and accepting the ~0.04 std as inherent. |

## Today's three questions

> **What did the agent actually discover?**
> The threshold-calibration step's `min(positive probability)` estimator over a small cal sample is the dominant *controllable* source of recall variance. The percentile swap (iters 17–19) confirmed this by cutting recall std 2.8×. However, full RNG seeding (iters 20–21) revealed a *structural* variance floor: the frozen `prepare.py` pipeline shuffles training data before model.py is ever imported, so the data order cannot be seeded from model.py. This means some run-to-run variance is irreducible within the project's constraints. The real levers going forward are (a) tuning the percentile to find a better recall/variance tradeoff, or (b) reporting mean ± std honestly rather than chasing a single-run peak.

> **Was the improvement real or accidental?**
> The AUC improvement (0.79 → 0.90) is real — reproduced across ~20 independent training runs, within-condition std ≈ 0.003. The recall improvement (0.90 → ~0.92 mean) is real on average but the two single runs that crossed 0.95 (iters 13 and 15) are accidental — their replicate siblings produced 0.888 and 0.892 respectively. Treating either as the project win would be the numbered Week-5 failure mode "the best run looked good, but I'm not sure why."

> **What kinds of modifications consistently failed?**
> Three categories: (1) multi-knob threshold tweaks that change two cal hyperparameters at once (iters 4, 5, 7, 9 — confounded, not attributable); (2) threshold-multiplier hacks like `SAFETY_MARGIN` (iter 8 — multiplied a noisy baseline, regressed); (3) the cal-data-source swap (iters 14–16 — fixed the wrong source of variance, *widened* the std).

## Why this trace is trustworthy

- **All runs logged** in `results.tsv` (auto-appended by frozen `run.py`) and re-organized in [`experiment_log_bundle.md`](experiment_log_bundle.md) with the operational events that aren't in `results.tsv`.
- **All commits on `main` and pushed to GitHub** at [`patrickgao2027/Stat390-Project`](https://github.com/patrickgao2027/Stat390-Project). Latest commit `155d0d3` includes iters 14–19.
- **Three controlled experiments**, each with one variable changed and ≥ 2 replicates per condition. None of them was a leaderboard chase; one rejected its hypothesis (week-5 priority 1, iters 14–16), one confirmed it (week-5 priority 1.5, iters 17–19), and one rejected its hypothesis but produced useful tradeoff curves (week 4, iters 10–13).
- **Two regressions caught and rolled back** with documented reasoning (iters 8, 9). The `run.py` default-`keep` mismatch is itself documented as part of the trace integrity.
